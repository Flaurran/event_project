{% extends '@App/layout/layout.html.twig' %}

{% block content %}
    <section class="form__wrapper">
        <div class="form__content">
            <a href="{{ path('project_manage', {'id': app.request.get('id')}) }}">Retour</a>
            <div class="help-text">
                <p>Il est uniquement nécessaire de remplir l'email si il correspond à celui d'un utilisateur. Sinon le prénom est également obligatoire.</p>
            </div>
            {{ form(form) }}
        </div>
    </section>
{% endblock %}